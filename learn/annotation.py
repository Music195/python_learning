#Annotation

def init_annotation(n: int) -> int: # meaning that n is an int and the return value is also an int"""
    """
    Initialize a number and return its incremented value.
    Args:
        n (int): The number to be initialized.  
    Returns:
        int: The incremented value of the number.
    """
    # n = n + 1 #Increment the number by 1
    return n +1

#help(init_annotation) #Print the help information for the init_annotation function

count: int = 0 #Create a variable with the value of 0